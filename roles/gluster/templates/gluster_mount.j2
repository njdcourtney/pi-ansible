[Unit]
Description=GlusterFS Mount {{ item.label }}
After=glusterd.service
Requires=glusterd.service

[Mount]
What=localhost:/{{ item.label }}
Where=/mnt/gluster/{{item.label }}
Type=glusterfs
Options=defaults,_netdev

[Install]
WantedBy=multi-user.target

