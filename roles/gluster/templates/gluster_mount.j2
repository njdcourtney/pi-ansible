[Unit]
Description=GlusterFS Mount
After=glusterfs-server.service
Requires=glusterfs-server.service

[Mount]
What=localhost:/{{ volume.key }}
Where=/mnt/gluster/{{volume.key}}
Type=glusterfs
Options=defaults,_netdev

[Install]
WantedBy=multi-user.target

